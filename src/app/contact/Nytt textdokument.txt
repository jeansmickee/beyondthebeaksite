export default function ContactPage() {
  return (
    <main className="mx-auto max-w-6xl px-6 py-14">
      <h1 className="font-serif text-4xl">Request a proposal</h1>
      <p className="mt-3 max-w-2xl opacity-80">
        Tell us your dates, interests and preferred pace — we’ll get back with a tailored itinerary and quote.
      </p>

      <div className="mt-10 max-w-xl rounded-2xl border border-black/10 bg-white/30 p-6">
        <p className="text-sm opacity-80">
          Next step: we’ll add the contact form that sends email (Resend) or stores requests (Supabase).
        </p>
      </div>
    </main>
  );
}